{
  "eval_id": 2,
  "eval_name": "dependency-resolver-analysis",
  "prompt": "Analyze dependency resolver for performance issues (O(n^2) patterns, redundant I/O)",
  "assertions": [
    {"text": "Identifies O(n^2) from .iter().any() inside loop", "type": "boolean"},
    {"text": "Suggests HashSet for O(1) membership checking", "type": "boolean"},
    {"text": "Identifies redundant fetch_dependencies calls for same package", "type": "boolean"},
    {"text": "Notes the resolver only handles 2 levels instead of proper graph traversal", "type": "boolean"},
    {"text": "Identifies unnecessary .to_string() allocations", "type": "boolean"},
    {"text": "Suggests caching parsed manifests", "type": "boolean"}
  ]
}
